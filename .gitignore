# dependencies
node_modules/
.pnpm-store/

# build output
.next/
out/
dist/
*.tsbuildinfo

# test / coverage
coverage/
playwright-report/
test-results/

# env / secrets (절대 커밋 금지)
.env
.env.*
!.env.example
# dev.sh가 생성하는 로컬 런타임 config (env.<stage>.json 템플릿만 커밋)
apps/web/public/env.json

# terraform
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
# 실제 값이 담기는 tfvars는 커밋하지 않는다 (.example만 커밋)
terraform.tfvars
*.auto.tfvars
# tf-backend.sh가 생성하는 원격 state 설정 (backend.hcl.example만 커밋)
infra/terraform/backend.hcl

# aws / local artifacts
*.tar.gz
*.tar.gz.sha256

# editor / os
.DS_Store
.idea/
.vscode/
*.log
